import styled from 'styled-components';

const Input = styled.input`
  flex: 1;
  padding: 0.4em;
  margin: 0.4em;
  border-radius: 20px;
  background: white;
  border: none;
`;

export default Input;
