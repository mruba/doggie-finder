// import { fromJS } from 'immutable';
// import { makeSelectMainGridDomain } from '../selectors';

// const selector = makeSelectMainGridDomain();

describe('makeSelectMainGridDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
